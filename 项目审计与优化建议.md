# 教AI导航 · 项目深度审计与优化方向

> 审计方式：逐文件读取 `ai-navigation-pro/front/src` 全部页面/组件/数据源（`data.ts` + 21 组件 + 16 路由），每条结论附 `文件:行号` 证据。审计时间：2026-08-03。
> 结论基调：产品信息架构与 SOP 体验是真实亮点；但在**跳转逻辑、数据来源准确性、个人中心可用性**三处存在可复现的硬缺陷。

---

## 一、你提的三个检查维度（逐条 + 证据）

### 1) 页面功能是否确实合理
| 页面 | 评估 | 证据/说明 |
|---|---|---|
| 搜索 `/search` | 基本合理但弱 | 仅对工具名/简介/学科/角色/场景名/用法标题做子串匹配，无排序无相关性打分；找不到"三年级""人教版"等粒度 |
| 全部工具 `/tools` | 合理 | 多维筛选 + 按评分排序到位（`ToolsBrowser.tsx`） |
| 用法详情 `/usages/[id]` | 合理（已修） | 直接复用工具真实 SOP，不再双份内容 |
| 个人中心 `/profile` | **不合理（断裂）** | 收藏 tab 读 `ea_favs` 但写入方写 `ea:fav`（见 C1）；笔记/反馈 tab 全站从未写入（见 C2） |
| 投稿 `/submit`、登录 `/login` | 演示态 | 仅 `alert`/`localStorage`，无后端（已注明"原型"） |
| 订阅（首页） | 空操作 | 提交无 handler |

### 2) 跳转页面是否符合逻辑
| 问题 | 严重度 | 证据 |
|---|---|---|
| `/usages?scene=xxx` 深链被忽略：`UsageBrowser` 用本地 `useState`，**不读 searchParams**，从场景点"查看分步做法"跳到用法库却不过滤 | 🔴 | `pages/home/page.tsx:196`、`scenes/page.tsx:182,54`、`tool/[slug]/page.tsx:176`、`SopPathView.tsx:150` |
| 首页"精选用法推荐"链到 `/tool/...` 而非新做的 `/usages/[id]` | 🟠 | `pages/home/page.tsx:230,264` |
| 搜索"用法 SOP"结果链到 `/tool/...` 而非 `/usages/[id]` | 🟠 | `search/page.tsx:163,174` |
| `ScenesBrowser.tsx` 是**死代码**（从未被 import），`/scenes` 实际跑的是 simpler 内联版 → phase-spine 滚动高亮（IntersectionObserver）没生效 | 🟠 | grep：`ScenesBrowser` 仅定义一处 |
| literacy "配套 SOP"链到工具页，标题暗示具体 SOP 但无对应深链 | 🟡 | `literacy/page.tsx:165` |

### 3) 渲染信息是否有真实获取途径（数据来源核查）
| 字段 | 问题 | 证据 |
|---|---|---|
| 用法"X 步 SOP"（`usage.steps`） | **与真实 SOP 步数大量不符**：9 条里 6 条错（u1 写3实2、u3 写3实2、u5 写2实1、u6 写3实2、u7 写3实1、u8 写2实1、u9 写3实2） | `data.ts:703-711` vs 各 path |
| 用法"有用/收藏"数字 | 写死常量（128/64…），且按钮**全站无任何点击逻辑**（只有 `data-kind` 属性，无 `addEventListener`/`onClick`）→ 纯装饰、不可交互 | grep `data-kind` 仅 `usages/page.tsx` 两处声明 |
| 首页 stats "3 覆盖角色" | 写死（实际 `ROLES` 有 4 个，且"学校管理员"无内容） | `pages/home/page.tsx:123` |
| 星级显示 | `Math.round(rating)` → 4.6 显示 5 星、4.2 显示 4 星，星视觉不反映小数（数字准） | `StarRating.tsx:10`、`tool/[slug]/page.tsx:128` |
| `学校管理员` 角色 | 无任何工具/场景归属，但出现在登录/投稿/角色筛选/场景 tab → 选它永远空结果（幽灵角色） | grep 全为声明，无数据归属 |

---

## 二、额外发现的硬缺陷（功能合理性）

- **C1 收藏键名不一致（真实 bug）**：`FavButton` 写 `localStorage["ea:fav"]`，`Profile` 读 `localStorage["ea_favs"]`（多一个 s）。结果：心形能点、有 toast，但个人中心"我的收藏"**永远为空**。
- **C2 笔记/反馈 tab 永远为空**：`ea_notes`、`ea_fb` 全站只被读取、从未被写入（`FeedbackBox` 只 `toast` 不落盘；无笔记写入入口）。
- **C3 "最新收录"排序是空操作**：`ToolsBrowser` 的 `useMemo` 仅处理 `rating/name`，`latest` 分支不排序，等于原数组顺序，标签误导。
- **C4 伪账号**：登录写 `localStorage`，"我是"角色存了却不参与任何个性化；换设备/清缓存全丢。
- 附：`literacy` 页内联重复 `ToolCard`，其收藏按钮是裸 `<button>` 无 handler，不可点。

---

## 三、战略层：如何让老师/学生/家长"离不开"

**老师（核心人群，SOP 已是资产）**
- 真实账号 + 跨设备同步 + "我的工作台/今日推荐" + SOP 进度续接（已有 localStorage 进度，已不错）
- 学科 × 年级更细的 SOP：目前 `subjects` 多为"综合"，数学仅 deepseek 一条，老师按学科筛几乎空
- "提示词工作台"：把 `{{变量}}` 填好即存为我的模板 —— 这是"离不开"的关键时刻
- 学校管理员角色落地 = 校方审核后向老师**推送精选 SOP**（幽灵角色的真正价值）

**学生（目前偏薄）**
- 仅 bishun/tongyi 真正面向学生；需：作业拆解模式、自学路径、防抄写引导式（笔神模式好）、自测题生成

**家长（几乎零专属内容）**
- 仅 xueqing/jiaxiao 角色标签；需：看懂孩子用的 AI、家庭 AI 使用公约、隐私安全指南、与老师沟通话术。点"家长"筛出来基本是老师工具，价值低

**信任与增长**
- 评分/有用/收藏是编辑写死，无真实众包信号 → 教师不会真信；需真实评价 + verified educator 标识
- 无每页 OG/结构化数据、订阅无后端 → 内容站要增长必须有 SEO + 邮件触达

---

## 四、优先级建议

**P0（修复即恢复信任，约 1–2 天）**
1. C1 收藏键名 `ea:fav`→`ea_favs`（或统一）
2. A1 用法深链：要么 `UsageBrowser` 读 `?scene=`，要么改链到 `/usages/[id]`
3. B1 `usage.steps` 改为从 `path.steps.length` 推导（删除错误字面量）
4. B2 让"有用/收藏"按钮可点（本地 +1 并持久化），或明确标注"编辑整理"
5. C2 给笔记/反馈加上写入落盘，或暂时隐藏空 tab

**P1（体验上台阶）**
- A2/A3 精选推荐与搜索结果链到用法详情页
- A5 路由改用 `ScenesBrowser`（恢复滚动高亮）或删除死代码
- B5 去"学校管理员"幽灵角色或补管理端内容
- B4 星级支持半星/小数视觉
- C3 修复"最新收录"排序（按收录序字段）
- 真实账号 + 跨设备同步

**P2（战略壁垒）**
- 提示词工作台（填变量→存模板→复制/运行）
- 学科 × 年级 SOP 矩阵
- 学校管理员推送精选 SOP
- 真实社区信号（评价/有用计数）
- 每页 OG/结构化数据 + 订阅后端
- 家长/学生专属内容线
