# AI通识课·官方桥接板块 需求规格（PM 交付）

> 输入文档：`AI通识课板块方案.md`（已定稿，2026-08-10 战略转向）、`front/src/app/literacy/page.tsx`（现状代码）、`项目审计与优化建议.md`、`教AI导航-后台管理系统方案.md`、`admin/prisma/schema.prisma`
> 战略前提（不推翻）：不自研课程，桥接国家中小学智慧教育平台《学AI》；本站做「导学桥接层」；数据 admin 手工录入；每条官方课一个独立伴学页 `/literacy/[module]/[lesson]`。
> 团队约束：1–2 人、近零预算、种子期口碑验证。**本规格所有取舍都以"运营者每天录得动"和"开发两周内做得完"为第一约束。**
> 缺失输入：`运营与推广方案-90天种子期.md` 在仓库中不存在（已 find 全盘确认），北极星 WAU-SOP=500/第12周 采用总监口述口径。

---

## 第一部分 · 内容运营侧字段需求

### 0. 先说结论：字段设计的唯一硬约束

**单条官方伴学课的录入耗时必须 ≤ 8 分钟。**

这不是拍脑袋。1–2 人团队，运营者一天能挤出的录入时间约 1 小时；开学窗口前要铺满 5 个模块，按每模块 4–5 条算需要 20–25 条。8 分钟/条 = 3 天铺完，还有余力写 SOP。若字段设计成 15 分钟/条，铺完要一周，运营者会开始"填空应付"——**字段填得敷衍比字段不存在更糟**，因为它会以"看起来有内容"的形式污染整个板块的质感。

所以下面每一个字段，我都问了同一个问题：**这个字段没有，用户会少得到什么？如果答案说不清楚，就砍掉。**

### 1. 实体一：LiteracyModule（模块）

现状是 `literacy/page.tsx:14-55` 里写死的 `MODULES` 常量数组（4 条），加一个模块要改页面代码。MVP 必须迁到数据表。

| 字段 | 类型 | 必填 | 长度建议 | 说明与理由 |
|---|---|---|---|---|
| `key` | string PK | 必填 | 小写英文短横线 | 如 `ai-basics`。进 URL：`/literacy/ai-basics`。**不用自增 id**，URL 里出现数字对 SEO 无益 |
| `num` | string | 必填 | 1 字 | 「一二三四五」，沿用现状视觉（`lit-num` 序号块） |
| `title` | string | 必填 | ≤12 字 | 如「AI 伦理与安全」。超过 12 字卡片会折行破版 |
| `desc` | string | 必填 | 40–70 字 | 沿用现状文案粒度（现有 4 条均在此区间，`page.tsx:18/28/38/48`） |
| `order` | int | 必填 | 默认 0 | 运营控制排序 |
| `status` | enum | 必填 | draft\|published | 模块整体可下架 |
| `intro` | text | **v2** | — | 模块页顶部长导读。MVP 复用 `desc`，因为运营写不动 5 篇导读 |
| `coverColor` | string | **v2** | — | MVP 用主题色轮换，不给运营选色（选色是设计师的活，不是运营的） |

MVP 数据量：5 条（模块一至五，含新增的「场景实战」）。**一次性录完，之后基本不动。**

### 2. 实体二：LiteracyLesson（官方伴学课）—— 核心实体

这是运营者每天要碰的表。字段分四组。

#### 2.1 官方课元信息（抄，不动脑）

| 字段 | 类型 | 必填 | 长度/格式 | 判断与理由 |
|---|---|---|---|---|
| `officialTitle` | string | **必填** | ≤40 字，**原样照抄不改写** | SEO 主关键词。老师在豆包里问「学AI 提示词 那节课」，命中的是官方原名。**禁止运营"优化"标题**——改了就搜不到，还有冒名嫌疑（见合规第 3 条） |
| `officialUrl` | string | **必填** | URL，**唯一约束** | 桥接的命脉。必须建 UNIQUE 索引：手工录入最高频的错误就是同一条课录两遍，靠人眼查重必然漏 |
| `slug` | string | **必填** | 小写英文短横线，模块内唯一 | 进 URL：`/literacy/ai-basics/what-is-llm`。**人工填英文，不做中文音译**——音译 slug 又长又丑且不稳定 |
| `stage` | enum[] | **必填** | 多选：小学/初中/高中/通用 | 教师的第一筛选反射。默认勾「通用」，运营改的成本≈1 秒 |
| `durationMin` | int | 选填 | 分钟 | 老师点外链前最想知道的一件事是「要占我多久」。官方页面上有就抄（≈5 秒成本），没标就留空。**留空时前端显示"时长以官方页面为准"而不是"0 分钟"** |

**明确不做的元信息（这是取舍的重点）：**

- **主讲人 / 主讲机构 —— 不做，且不是 v2，是永不做。** 《学AI》不是名师大课，老师不按讲师选课。存了它意味着：录入多一个字段（+30 秒/条 × 25 条 = 12 分钟）、官方换人要维护、还可能出现姓名写错的事故。**这个字段的全部价值，用户点外链进去 3 秒就看到了。**
- **课程封面图 —— v2。** MVP 用「模块色块 + 序号 + 标题」的纯文字卡。理由有二：一是官方封面图属于官方作品，扒下来存本站是明确的侵权动作（合规第 1 条），自己做图 = 运营每条多花 10 分钟；二是纯文字卡在教师用户群里并不掉转化，老师看的是课名和「学完能干嘛」。
- **官方课发布时间 / 版本号 —— v2**，配合链接巡检一起做。
- **观看人数 / 官方评分 —— 不做。** 抓不到，编造就是造假。

#### 2.2 本站原创部分（唯一的差异化，也是唯一要动脑的地方）

这三个字段决定了本站是「有价值的导学层」还是「一个外链农场」。字数建议是核心——**太长运营写不动，太短没价值**，我按"运营者一次能一口气写完不卡壳"的粒度定：

| 字段 | 类型 | 必填 | **字数建议** | 定这个字数的理由 |
|---|---|---|---|---|
| `guideText`（一句话导学：为什么你该看这条） | text | **必填** | **40–80 字，硬性校验下限 40** | 80 字 ≈ 卡片两行，是运营者不用打草稿、一口气能写完的极限。低于 40 字必然写成「本课介绍提示词的基本用法」这种废话——所以下限要硬校验。这是全站唯一会被豆包/元宝抓去当答案的原创句子，**它的质量就是 AISO 的天花板** |
| `takeaway`（学完你能拿走什么） | text[] | 选填 | **2–3 条，每条 15–25 字** | 结构化短句比长段落好写十倍（"能自己写出带角色设定的提示词"）。设为选填，但前端在有值时优先展示——**让运营尝到"写了效果明显更好"的甜头，比强制必填更能拿到质量** |
| `comment`（本土化点评 / 老师视角补充） | text | 选填 | **60–150 字** | 最有价值也最慢的字段，平均要 3–5 分钟。**绝不能设必填**，否则运营一卡壳整条录入就停摆。策略：MVP 只给"重点课"写（模块三伦理类必写，其余随缘），后台列表页用一个标记提示哪些课还缺点评，当作待办池慢慢补 |

> **写作语态硬要求（写进后台字段提示文案，不是写进文档）**：用「你」称呼老师，写具体场景不写抽象概念。
> 正面样例（可直接作为后台占位提示）：`把"帮我写个教案"改成 AI 真能听懂的指令——这节课讲的三段式，就是你写提示词老是不满意的根因。`
> 反面样例（后台提示里明确列出禁止）：`本课程系统介绍了提示词工程的基本概念与应用方法。`

#### 2.3 关联关系（桥接模式的立命之本）

| 字段 | 类型 | 必填 | 说明与理由 |
|---|---|---|---|
| `moduleKey` | string FK | **必填** | 归属模块，单选 |
| `relatedSopIds` | string[] | **必填，且发布态至少 1 条** | **这条是整个字段设计里我最坚持的硬校验。** 一条只有外链没有 SOP 的伴学页，对用户等于"绕一圈还是去了别人家"，对搜索引擎等于低质跳板页。「学完原理→立刻动手练」这个闭环，物理载体就是这个字段。**没挂 SOP 就不许发布**，宁可少发几条 |
| `relatedToolSlugs` | string[] | 选填 | 0–3 个。锦上添花，SOP 详情页里本来就有工具入口，不必强求 |

> **数据格式对齐（给架构师）**：`relatedSopIds` 存的是前台 `/usages/[id]` 的 id（`content.ts:73` 的 `Usage.id`，即 `admin/prisma/schema.prisma:74` 的 `SopPath.usageId`），**不是 tool slug**。这正是现状 bug 的根因——`literacy/page.tsx:11` 的类型定义是 `sops: { tool: string; title: string }[]`，从数据结构层面就存错了东西，链接自然只能指向工具页。**修 bug 不是改一行 href，是改数据模型。**

#### 2.4 运营控制位

| 字段 | 类型 | 必填 | 说明 |
|---|---|---|---|
| `status` | enum | **必填** | `draft` \| `published`，默认 draft。**不做 review 态**——1–2 人团队，自己审自己是形式主义。（UGC 投稿走已有的 `/submit` review 流，与本表无关） |
| `order` | int | 选填 | 模块内排序，默认 0 |
| `isFeatured` | bool | 选填 | 落地页「精选伴学课」推荐位，默认 false。**限制最多 6 条**，超出时后台报错而不是静默截断 |
| `createdAt` / `updatedAt` | datetime | 系统 | 自动 |
| `linkCheckedAt` / `linkStatus` | datetime / enum | 系统 | 链接巡检回写，运营只读不填（见 AC-19/20） |

#### 2.5 明确进 v2 的字段（附砍掉的理由）

| 字段 | 为什么 MVP 不做 |
|---|---|
| `subject` 学科 | **这条要顶住诱惑。** 总监问"教师会怎么找课？按'我是语文老师'？"——会这么想，但**给通识课打学科标签是伪精度**。《学AI》讲的是原理和红线，跨学科通用；一个语文老师筛完发现内容和数学老师看的一模一样，信任就掉了。**学科维度属于 SOP，不属于通识课**。老师的"我是语文老师"这个诉求，MVP 用伴学页底部的关联 SOP 满足（那里有真实学科属性），不用课程筛选器满足 |
| `level` 难度 | 与 `stage` 高度重合，MVP 是冗余信息 |
| `keywords` / `metaDescription` | MVP 用 `officialTitle` + `guideText` 自动生成 meta。给运营多两个 SEO 字段，结果一定是填得比自动生成还差 |
| `assetFiles` 可下载资产 | 方案第 2.2 节的钩子（清单/模板 PDF），是**自研内容线**的字段，不属于"官方伴学课"表。混在一起会让运营以为每条官方课都要配资产 |
| 阅读量 / 收藏数展示 | 种子期真实数据是个位数，展示出来是负向社会证明。等有量了再开 |

#### 2.6 筛选维度：MVP 只给两个

**模块（5 个）+ 学段（4 个）**，就这两个。

理由是算出来的：MVP 预计 20–25 条课，5 个模块摊下来每模块 4–5 条。此时再加第三个筛选器，任意两个条件一叠加结果就剩 0–1 条，**用户得到的是空结果页，不是精准结果**。筛选器的价值和内容量是正相关的，内容量不够时筛选器是负资产。

内容量到 60 条以上（对齐运营方案里 SOP 扩到 60 条的节点）再考虑加维度。

### 3. 录入负担核算（验证 8 分钟目标）

| 动作 | 耗时 |
|---|---|
| 粘贴官方标题 + URL + 时长 | 40 秒 |
| 选模块 / 学段 / 排序 | 20 秒 |
| 填 slug | 30 秒 |
| **写 guideText（40–80 字）** | **3 分钟** |
| **挑 relatedSopIds（1–3 条）** | **2 分钟** |
| 存草稿 → 预览 → 发布 | 40 秒 |
| **合计（不含选填 comment）** | **≈ 7.5 分钟，达标** |
| 若加写 comment（60–150 字） | +4 分钟 → 11.5 分钟（所以它必须是选填） |

> 给架构师的一条产品要求：后台录入表单必须支持**从 URL 粘贴后自动填 slug 建议值**（取官方 URL 末段做 transliterate 提示，运营可改）。这一个小功能省 30 秒/条，25 条省 12 分钟——**在 1–2 人团队里这不是小数**。

---

## 第二部分 · EARS 格式验收标准

> 格式：`[While/When/If/Where] <条件> , 系统 必须/应该 <行为>`
> 必须 = 不满足即不可上线；应该 = 期望但可让步。
> 优先级：P0 = 阻断上线；P1 = 首个迭代内补齐。

### A 组 · 三层结构的展示与导航

| 编号 | 验收标准 | 优先级 |
|---|---|---|
| **AC-01** | 系统**必须**提供三层路由：落地页 `/literacy`、模块页 `/literacy/[module]`、伴学页 `/literacy/[module]/[lesson]`，三层均为服务端渲染且可被搜索引擎独立抓取。 | P0 |
| **AC-02** | When 用户访问 `/literacy`，系统**必须**展示全部 `status=published` 的模块卡片，每张卡片显示序号、标题、简介、该模块下已发布伴学课数量，且整卡可点击进入模块页。（现状 `literacy/page.tsx:106` 的模块块不可点击，是本次必须消除的缺陷） | P0 |
| **AC-03** | When 用户访问 `/literacy/[module]`，系统**必须**按 `order` 升序、`order` 相同时按 `createdAt` 降序，列出该模块下全部 `status=published` 的伴学课。 | P0 |
| **AC-04** | When 用户访问 `/literacy/[module]/[lesson]`，系统**必须**在首屏依次展示：官方课标题、学段标签、时长（无值时显示「时长以官方页面为准」）、导学文案、前往官方平台的外链按钮。 | P0 |
| **AC-05** | Where 伴学课存在 `takeaway` 值，系统**必须**以列表形式展示「学完你能拿走」区块；Where 无值，系统**必须**整块隐藏而非显示空标题。 | P0 |
| **AC-06** | Where 伴学课存在 `comment` 值，系统**必须**展示「编辑点评」区块并标注撰写方为教AI导航编辑；Where 无值，系统**必须**整块隐藏。 | P0 |
| **AC-07** | 系统**必须**在模块页与伴学页提供面包屑导航「AI通识课 / 模块名 / 课程名」，其中前两级可点击返回。 | P1 |
| **AC-08** | When 用户在伴学页浏览到底部，系统**应该**展示同模块内的上一课 / 下一课导航；若为首条或末条，对应入口隐藏。 | P1 |
| **AC-09** | When 用户在 `/literacy` 落地页，系统**必须**保留现有跨板块引导条（去 `/scenes` 找工具、去 `/usages` 看 SOP），维持现状信息架构不倒退。 | P1 |
| **AC-10** | 系统**必须**为每个伴学页输出独立的 `<title>`、`meta description`（由 `officialTitle` + `guideText` 生成）与 canonical 指向本站自身 URL。 | P0 |
| **AC-11** | 系统**不得**为伴学页设置 canonical 指向国家平台 URL，也**不得**添加 `noindex`。（伴学页是本站 SEO/AISO 核心资产，把权重让渡给官方站等于自废武功） | P0 |

### B 组 · 官方外链的打开行为与来源标注

| 编号 | 验收标准 | 优先级 |
|---|---|---|
| **AC-12** | When 用户点击「前往国家平台观看」按钮，系统**必须**在新标签页打开 `officialUrl`，且链接带 `target="_blank"` 与 `rel="noopener noreferrer"`。（不带 noopener 存在反向标签劫持风险，不带 noreferrer 会把本站 URL 泄露给对方日志） | P0 |
| **AC-13** | 系统**必须**在外链按钮上同时呈现文字「前往国家中小学智慧教育平台观看」与新窗口图标（SVG 图标，**不使用 emoji 字符**），使「即将离站」在点击前可预期。 | P0 |
| **AC-14** | 系统**必须**在外链按钮下方常驻展示来源声明（文案见第三部分 3.2），该声明**不得**折叠、**不得**放入弹层、**不得**需要滚动才可见。 | P0 |
| **AC-15** | When 用户点击外链，系统**必须**先记录 `official_link_click` 事件再执行跳转，且埋点失败**不得**阻塞跳转（使用 `navigator.sendBeacon`）。 | P0 |
| **AC-16** | 系统**不得**在本站页面内以 iframe 或任何嵌入方式呈现国家平台页面。 | P0 |

### C 组 · 关联 SOP 的跳转（含现存 bug 修复）

| 编号 | 验收标准 | 优先级 |
|---|---|---|
| **AC-17** | When 用户点击伴学页「配套动手练」中的任一 SOP 条目，系统**必须**跳转至该 SOP 的详情页 `/usages/{sopId}`，**不得**跳转至工具页 `/tool/{slug}`。<br>**（回归缺陷修复：现状 `front/src/app/literacy/page.tsx:129` 为 `href={\`/tool/${sop.tool}\`}`，标题写的是具体 SOP 而落地在工具首页，与审计报告 A 组「literacy 配套 SOP 链到工具页」一致。修复须落到数据模型：`literacy/page.tsx:11` 的 `sops: { tool: string; title: string }[]` 改为存 `Usage.id` 数组。）** | **P0** |
| **AC-18** | If 某个 `relatedSopIds` 中的 id 在内容源中查无对应 SOP，系统**必须**在前台静默跳过该条目而非渲染死链，同时在后台该课的编辑页标红提示「关联 SOP 已失效」。 | P0 |
| **AC-19** | When 用户从伴学页进入 SOP 详情页，系统**必须**记录来源为通识课（`related_sop_click` 事件带 `lesson_id`），并在客户端写入 7 天有效的通识课归因标记（见第四部分 4.4）。 | P0 |
| **AC-20** | When 用户在伴学页点击 SOP 条目，系统**应该**在同标签页内跳转（站内导航不开新窗口，与站内其他链接行为一致），与外链的开新窗口行为形成可感知的区分。 | P1 |

### D 组 · 外链失效的降级行为

| 编号 | 验收标准 | 优先级 |
|---|---|---|
| **AC-21** | 系统**必须**提供定时链接巡检任务（建议每日一次），对全部 `published` 伴学课的 `officialUrl` 发起可用性检查，并回写 `linkStatus`（`ok` \| `broken` \| `unknown`）与 `linkCheckedAt`。 | P1 |
| **AC-22** | While 某伴学课 `linkStatus=broken`，系统**必须**在其伴学页把外链按钮置为不可点击的禁用态，并展示提示「该课程在官方平台的链接暂时无法访问，我们正在核实。你可以先看下方的配套动手练。」 | P0 |
| **AC-23** | While 某伴学课 `linkStatus=broken`，系统**必须**仍然完整渲染导学文案与关联 SOP 区块，**不得**整页 404 或整页跳转。（外链挂了，本站的原创导学与 SOP 承接依然有价值，这是「不镜像」模式下唯一的抗风险资产） | P0 |
| **AC-24** | While 某伴学课 `linkStatus=broken`，系统**必须**在模块页该课卡片上显示「官方链接核实中」角标，且该课**必须**自动排到列表末尾。 | P1 |
| **AC-25** | When 链接巡检把任一课标记为 `broken`，系统**应该**在后台仪表盘的待办区列出该课，供运营手工核实后改回或下架。 | P1 |
| **AC-26** | 系统**不得**因为链接失效而自动把课程改为 `draft`。（自动下架会让运营在毫不知情的情况下丢内容，且官方站的临时抖动会造成误杀——降级展示优于自动删除） | P0 |

### E 组 · 后台录入与发布状态机

| 编号 | 验收标准 | 优先级 |
|---|---|---|
| **AC-27** | 系统**必须**实现 `draft → published → draft` 双向状态机，新建记录默认 `draft`；**不得**引入 review 中间态。 | P0 |
| **AC-28** | When 运营在后台把一条伴学课从 `draft` 改为 `published`，If 以下任一必填项缺失，系统**必须**拒绝发布并逐条列出缺失字段名：`officialTitle`、`officialUrl`、`slug`、`stage`（≥1）、`moduleKey`、`guideText`（≥40 字）、`relatedSopIds`（≥1 条）。 | P0 |
| **AC-29** | If 运营录入的 `officialUrl` 与库中任一已存在记录重复，系统**必须**在保存时拒绝并提示「这条官方课已录入：《{已有课程标题}》」，附跳转到该记录的链接。 | P0 |
| **AC-30** | If 运营录入的 `officialUrl` 的域名不属于白名单（`basic.smartedu.cn`、`smartedu.cn` 及其子域），系统**必须**给出二次确认提示「该链接不是国家中小学智慧教育平台域名，确认要作为官方伴学课录入吗？」，确认后方可保存。（防止把第三方转载源当官方录进来——这是合规事故的最高频入口） | P0 |
| **AC-31** | While 一条伴学课处于 `draft`，系统**必须**保证其在前台落地页、模块页、站内搜索、sitemap 中均不可见；同时**必须**支持通过带令牌的预览链接查看完整渲染效果。 | P0 |
| **AC-32** | When 运营在后台保存伴学课，系统**必须**校验 `guideText` 长度在 40–160 字之间（下限硬拦，上限给 2 倍冗余避免误伤），超界时给出字数提示而非静默截断。 | P0 |
| **AC-33** | When 运营在后台把一条已发布伴学课改回 `draft`，系统**必须**在 60 秒内使前台该页返回 404（或 410），并从 sitemap 中移除。 | P1 |
| **AC-34** | When 运营勾选 `isFeatured` 且当前已有 6 条精选，系统**必须**拒绝并提示「首页精选位已满（6/6），请先取消一条」，**不得**静默截断展示。 | P1 |
| **AC-35** | 系统**不得**对国家平台运行任何自动抓取 / LLM 采集流程。现有 `admin/src/app/api/admin/collect/route.ts` 的采集能力仅适用于工具库，**必须**在通识课模块中不可达（后台不暴露入口）。 | P0 |

### F 组 · 空数据态

| 编号 | 验收标准 | 优先级 |
|---|---|---|
| **AC-36** | While 某模块下已发布伴学课数为 0，系统**必须**在模块页展示空状态：一句说明「这个模块的伴学课正在整理中」+ 一句去向引导 + 两个出口按钮（「先去看动手练 SOP」→ `/usages`、「回到全部模块」→ `/literacy`）。**不得**展示空白区域或纯文字「暂无数据」。 | P0 |
| **AC-37** | While 某模块下已发布伴学课数为 0，系统**必须**在落地页该模块卡片上显示「整理中」状态标识，且卡片**仍然可点击**（进入后看到 AC-36 的空态与出口）。（把空模块做成不可点击会让用户以为站点坏了） | P0 |
| **AC-38** | While 全站已发布伴学课数为 0，系统**必须**让落地页降级为「模块骨架 + 说明本板块定位 + 引导去用法库」的形态，**不得**出现空的列表容器。 | P1 |
| **AC-39** | While 某伴学课的 `relatedSopIds` 在渲染时全部失效（配合 AC-18），系统**必须**用「这节课的配套动手练正在补充，先去用法库看看 →」替换该区块，**不得**渲染空标题。 | P0 |
| **AC-40** | 系统**必须**为空状态文案使用教师语态的具体表达，**不得**出现「Welcome to」「暂无内容」「Lorem ipsum」一类占位式文案。 | P0 |

### G 组 · 合规声明的常驻展示

| 编号 | 验收标准 | 优先级 |
|---|---|---|
| **AC-41** | 系统**必须**在每一个伴学页的外链按钮附近常驻展示课程来源声明（文案见 3.2 第①条），无需交互即可看见。 | P0 |
| **AC-42** | 系统**必须**在 `/literacy` 落地页与每个模块页页尾常驻展示桥接说明（文案见 3.2 第②条）。 | P0 |
| **AC-43** | 系统**必须**新建 `/compliance` 合规声明页并承载完整声明全文（3.2 全部条款）。<br>**（回归缺陷修复：现状 `front/src/components/Footer.tsx:35` 的「合规声明」为 `href="#"` 死链，在桥接官方内容的场景下，一个点不开的合规声明本身就是风险点。）** | **P0** |
| **AC-44** | 系统**必须**在伴学页展示「导学文案、编辑点评与配套 SOP 由教AI导航编辑撰写」的署名说明，使原创部分与官方内容在视觉上可区分（分区块 + 不同底色 + 明确小标题）。 | P0 |
| **AC-45** | 系统**必须**在页脚常驻非官方关系声明（3.2 第④条），使任意页面均可见。 | P0 |
| **AC-46** | 系统**必须**在伴学页与 SOP 详情页展示学生数据红线提示（3.2 第⑤条）。 | P0 |
| **AC-47** | 系统**必须**提供全局配置开关，可一键将全部官方伴学课下线（详见 3.5 下架预案），且该操作**必须**在 10 分钟内于前台生效。 | P0 |

---

## 第三部分 · 合规与法律边界清单

> 定位：本板块要把**教育部直属平台的内容**当作产品的一部分展示，这是全项目法律风险最高的一块。下面每一条都写成**开发能改成代码的动作**，不写「要注意合规」这种话。

### 3.1 「只外链不镜像」到底意味着什么（可落到 DB 层的红线）

**绝对不能进本站数据库的内容**（架构师建表时应视为禁止字段，代码评审的否决项）：

| 禁止存储 | 具体指 | 为什么 |
|---|---|---|
| 课程视频文件 / 音频 | 任何 mp4、m3u8、转存的 CDN 地址 | 复制传播权，无授权 |
| 课程视频的直链、可嵌入播放地址 | 从官方页面抓出来的媒体地址 | 绕过官方页面 = 深度链接盗链，性质等同镜像 |
| 课程字幕 / 逐字稿 / 讲义 PDF | 含 AI 转写生成的文字稿 | 转写件是演绎作品，无授权 |
| 课程正文的整段摘录 | 连续超过 100 字的原文 | 超出合理引用范围 |
| 官方课程封面图 / 截图 | 含运营手动截的图 | 美术作品 + 平台界面权益 |
| 官方平台 Logo、「智慧教育平台」标识 | 任何形式使用 | 商标 + 会造成官方背书的误认 |

**允许存储的全集**（除此以外一律不存）：`officialTitle`（客观标题，事实性信息）、`officialUrl`、`durationMin`（客观事实）、`stage`（本站的分类判断）、以及**全部由本站编辑原创撰写**的 `guideText` / `takeaway` / `comment`。

**三条实现层要求：**
1. **代码级**：`LiteracyLesson` 表**不得**包含任何 `content` / `body` / `transcript` / `videoUrl` / `coverImage` 字段。此约束写入 schema 注释，作为后续迭代的护栏。
2. **渲染级**：不使用 iframe、不使用 `<video>` 指向官方资源、不做任何形式的反向代理（AC-16）。
3. **爬虫级**：不对 `basic.smartedu.cn` 运行任何抓取程序，包括「只抓标题」的轻量脚本（AC-35）。数据一律手工录入——这既是总监的决策，也是本条合规的实现方式。

### 3.2 来源声明文案（以下为可直接使用的中文原文，开发照抄）

**① 伴学页 · 外链按钮下方常驻（AC-41）**
```
课程来源：国家中小学智慧教育平台（basic.smartedu.cn）。本页仅提供课程导航与配套练习推荐，
不提供课程内容本身。点击后将跳转至官方平台观看，课程内容与最终解释权归官方平台所有。
```

**② 落地页 / 模块页 · 页尾常驻（AC-42）**
```
本板块的通识课程均来自国家中小学智慧教育平台《学AI》专栏，由教AI导航整理导航路径，
并为每节课配上本站的分步操作 SOP，方便你学完原理就能上手练。课程本身请到官方平台观看。
```

**③ 伴学页 · 原创部分署名（AC-44，置于导学/点评区块的小标题旁）**
```
以下导学、点评与配套动手练由教AI导航编辑撰写，仅代表本站观点，不代表国家中小学智慧教育平台立场。
课程内容以官方平台实际发布为准。
```

**④ 全站页脚 · 非官方关系声明（AC-45）**
```
教AI导航是独立运营的教师 AI 工具导航站，与国家中小学智慧教育平台无隶属、合作或授权关系，
不代表任何官方机构。站内引用的官方课程链接仅用于方便教师学习，版权归原平台所有。
```

**⑤ 伴学页 / SOP 详情页 · 学生数据红线提示（AC-46）**
```
使用任何 AI 工具时，请勿输入学生的真实姓名、学号、成绩、照片、家庭住址与联系方式。
需要举例时请用「学生A」「某同学」代替。这是教育场景的红线，也是保护你自己。
```

**⑥ `/compliance` 合规声明页 · 页首（AC-43）**
```
教AI导航是一个由一线教育从业者独立运营的公益向导航站点。我们不生产课程，
不镜像任何第三方内容，站内所有外部课程与工具均以链接形式导向其官方来源。
如果你是内容或平台的权利方，认为本站的引用方式不妥，请通过页面底部的联系方式告知，
我们会在收到通知后 24 小时内处理。
```

> 上述文案已按教师语态撰写，无 emoji、无模板占位语。**开发不得自行改写**，如需调整须回到 PM。

### 3.3 如何避免用户误认为本站是官方站点 / 官方合作方

这是本板块**最容易翻车、也最容易被举报**的一点。老师对「官方」二字极其敏感，一旦感觉被骗，口碑立刻反转——而口碑正是种子期的唯一目标。

**开发必须实现的六个动作：**

| # | 动作 | 实现要求 | 优先级 |
|---|---|---|---|
| 1 | 禁用官方视觉资产 | 不得使用国家平台 Logo、配色、界面截图；伴学页整体沿用本站既有视觉系统 | P0 |
| 2 | 措辞白名单 | 全站文案只允许「课程来源」「来自」「链接至」「整理自」；**禁用**「合作」「联合出品」「授权」「官方指定」「战略伙伴」「入驻」 | P0 |
| 3 | 页面标题不冒名 | 伴学页 `<title>` 必须为「{课程名} · 配套动手练 - 教AI导航」这类结构，本站品牌必须出现；**不得**出现「国家中小学智慧教育平台 - {课程名}」这种把官方名放主位的写法 | P0 |
| 4 | 外链离站可预期 | 新窗口 + 文字 + 图标三重提示（AC-12/13），让老师在点击前就知道要去别处 | P0 |
| 5 | 原创与官方视觉隔离 | 导学/点评区块必须有独立底色与「教AI导航编辑」署名（AC-44），使读者一眼能分清「哪句是官方说的、哪句是这个站说的」 | P0 |
| 6 | 结构化数据不冒领 | 伴学页若输出 Schema.org 标记，`Course` 的 `provider` **必须**填国家中小学智慧教育平台，`author` 填本站；**不得**把本站填成课程提供方 | P1 |

> 特别提醒开发：`officialTitle` 要求原样照抄（第一部分 2.1），但**页面 H1 不得单独只放官方课名**，必须与本站品牌或「伴学」定语组合出现，否则搜索结果里会呈现为本站冒充官方课程页。

### 3.4 涉及未成年人与学生数据的额外要求

本站主用户是教师（成年人），但内容天然指向未成年人场景，这带来两类要求：

| 要求 | 开发动作 | 优先级 |
|---|---|---|
| 站点定位声明 | `/compliance` 页与注册页明示「本站面向教师及教育从业者，不面向 14 周岁以下未成年人提供注册与个性化服务」 | P0 |
| 不采集未成年人信息 | 全站表单（投稿 `/submit`、反馈、注册）**不得**出现学生姓名、班级、学号、年龄字段；已有表单需复核 | P0 |
| 内容侧红线提示 | 3.2 第⑤条声明在伴学页与 SOP 详情页常驻（AC-46） | P0 |
| SOP 提示词模板去标识化 | 所有可复制的提示词模板中，凡涉及学生信息的位置**必须**使用 `{{学生化名}}` 占位而非真实姓名示例；后台保存 SOP 时对疑似真实姓名给出提醒 | P1 |
| 埋点不采个人信息 | 事件上报仅含匿名设备 ID，**不采集** IP 原文、UA 原文、任何输入内容（详见第四部分 4.5） | P0 |
| UGC 审核加一条 | 教师投稿审核清单增加「是否含学生个人信息」检查项，命中即驳回 | P1 |

### 3.5 如果国家平台方要求下架：应对预案

**必须在上线前就做好，不能等收到函再开发。** 收到通知到完成处置的目标时间：**2 小时内**。

| 层级 | 处置动作 | 技术实现 | 生效时间要求 |
|---|---|---|---|
| **L1 单条下架** | 把指定伴学课改为 `draft` | 后台常规操作 + AC-33 的 60 秒生效 | 5 分钟 |
| **L2 全板块停展** | 全局开关 `LITERACY_BRIDGE_ENABLED=false` | 站点配置表读取，关闭后：全部 `source=official` 的伴学页返回 410、模块页隐藏官方课、落地页降级为 AC-38 形态 | **10 分钟（AC-47）** |
| **L3 彻底清除** | 删除全部官方课记录 | 后台批量删除 + sitemap 重建 + 主动向搜索引擎提交移除 | 2 小时 |

**配套要求：**
1. **联系通道必须真实可用**——`/compliance` 页与页脚必须有一个真实在收信的邮箱。（现状 Footer 的「联系我们」同为 `href="#"` 死链，须一并修复）
2. **保留录入日志**——每条伴学课保留 `createdBy` / `createdAt` / `updatedAt`，以便在争议时说明本站从未存储过课程内容本身。
3. **L2 之后本站不会归零**——这正是「必填至少 1 条关联 SOP」这条硬校验的战略价值：即使官方课全部撤下，本站的导学文案与 SOP 承接仍然是自有资产，板块降级但不死。**这是把合规风险转化为架构约束的关键一笔。**

---

## 第四部分 · 成功指标与埋点需求

### 4.0 先纠正一条现状认知（重要，影响排期估算）

总监给的背景是「全站'有用/收藏'按钮只有 `data-kind` 属性、没有任何点击逻辑，全站零行为数据」。**这条信息来自 2026-08-03 的审计报告，现在已经部分过时了**，实际读代码的结论是：

| 项 | 审计时（08-03） | 现在 | 证据 |
|---|---|---|---|
| 有用/收藏按钮 | 死按钮，无 handler | **已修复，且已落服务端** | `front/src/lib/interactions.ts` 全部走 `authFetch('/api/me/favorites' \| '/api/me/useful' \| '/api/me/ratings')`；`admin/src/app/api/me/` 下六个接口均已建 |
| 行为数据 | 零 | **仍然接近零，但原因变了** | 见下 |

**真实的缺口是这三条，和「按钮能不能点」无关：**

1. **`/api/me/*` 全部走 `authFetch`，即需要登录。** 种子期绝大多数老师不会注册，所以有用/收藏虽然能点、能落库，但**只覆盖了登录用户这一小撮**，匿名教师的行为依然全部丢失。
2. **完全没有匿名行为事件采集。** 页面浏览、外链点击、SOP 打开——这三个构成漏斗的动作，一个都没有记录。
3. **最要命的一条：北极星定义里的「复制了提示词」这个动作，本身没有任何埋点。** `front/src/components/CopyButton.tsx` 的 `copyText()` 从头到尾只做剪贴板写入，无任何上报（`grep` 全文件无 fetch / beacon / track）。

> **结论直给架构师**：WAU-SOP 这个北极星，**以当前代码根本算不出来**。不是"数据不准"，是"这个数字压根不存在"。做通识课桥接的同时必须补一条最小事件通道，否则第 12 周做复盘时没有任何东西可看。这条要提到和功能开发同等优先级。

### 4.1 本板块要回答的四个核心业务问题

| # | 业务问题 | 说人话 | 判据 |
|---|---|---|---|
| **Q1** | **桥接模式到底成不成立？** | 老师看完国家平台的原理课，会不会回到本站来动手练？ | 外链点击者的 7 日回站转化率 ≥ 12%（4.4 定义） |
| **Q2** | 官方背书是否真的带来信任溢价？ | 挂"国家平台"的课，比本站自己写的内容更能让老师往下走吗？ | 官方伴学课的「导学页 → SOP 点击率」显著高于同期站内其他入口的 SOP 点击率 |
| **Q3** | 哪些课值得继续录？ | 25 条课里哪 5 条在干活，哪 15 条白录了？ | 单课的「浏览 → 外链或 SOP 动作」转化率排序；连续 4 周转化为 0 的课进淘汰池 |
| **Q4** | 导学文案有没有用？ | 花 3 分钟写的 80 字，值不值？ | 有 `comment` 的课 vs 无 `comment` 的课，SOP 点击率差异（自然实验，不用做 A/B——种子期流量不够跑 A/B，硬跑只会得到噪声） |

### 4.2 关键判断：怎么定义「成功」才不会被表面数据误导

这是本板块最容易做错决策的地方，我单独展开。

**问题的本质**：老师点外链去了国家平台，本站的会话就结束了。在任何常规分析口径里，这是「跳出」；在 GA 里这是「高跳出率 + 低停留时长」；如果拿这套指标做周报，团队会得出「通识课板块表现差，应该砍掉」的结论。

**但业务上它恰恰是成功——因为本站的产品定义就是"送老师去看权威课，然后接住他"。跳出是产品设计的目的，不是产品的失败。**

如果被表面数据带着走，团队会做出三个典型的错误优化，每一个都在杀死这个板块：
- 把外链改成弹窗/iframe 留住用户 → 直接踩合规红线（3.1）
- 在伴学页堆本站自己写的内容降低跳出 → 回到"自研课程"老路，违背既定战略
- 用"外链点击后 30 分钟内回站率"做 KPI → 等于鼓励老师**不看完课就回来**，把好指标做成坏产品

**我的定义：本板块的成功是「送达 + 回收」，不是「停留」。**

```
成功 = 把老师准确送到官方课（送达） + 他学完之后回到本站动手练（回收）
     ↑ 立刻可测                        ↑ 跨会话、7 天窗口才可测
```

**落到一个可算的数：** 定义板块专属北极星贡献指标

> **`literacy_assisted_WAU-SOP`** = 一个自然周内，满足以下两个条件的**去重匿名设备数**：
> （A）该设备在过去 **7 天内**曾发生 `official_link_click` 或 `lesson_view_30s` 中的任一事件；
> （B）该设备在本周内完成了至少一次 `sop_prompt_copied`。
>
> 归因窗口 **7 天、跨会话、跨天**。技术实现：点外链或读满 30 秒时在 first-party storage 写 `ea_lit_ref = {lessonId, ts}`，之后任意时刻发生 `sop_prompt_copied` 时把 `lit_ref_lesson_id` 与 `lit_ref_age_h` 带上。

**为什么归因窗口必须是 7 天而不是当次会话**：老师的真实行为节奏是「周三晚上刷到一条伴学课，点开官方视频看了二十分钟，周末备课时才想起来回来抄提示词」。**用会话级归因，这个人 100% 会被记成跳出；用 7 天窗口，他被正确地记成一次成功。** 桥接模式的全部价值就藏在这个时间差里，口径选错了，整个板块的成绩会被系统性地记成零。

**同时明确三条「不做 KPI」的反指标**（写进文档是为了防止后面有人拿它们做汇报）：

| 反指标 | 为什么不能用 |
|---|---|
| 伴学页跳出率 | 跳出即达成产品目的，越低越可疑 |
| 伴学页平均停留时长 | 导学文案就 80 字，读完 30 秒就该走，长停留意味着老师在页面上迷路 |
| 外链点击后 30 分钟内回站率 | 优化它 = 鼓励老师不看完课就回来，反向优化 |

**最后，也是最重要的：明确承认三件事我们测不到，并且不假装能测。**
官方站内的完播率、官方站内停留时长、老师是否真的看懂了——这三个数据在国家平台手里，本站永远拿不到。**不要为此做任何技术尝试（那会变成抓取行为，见 3.1）**。代理指标只有一个：**回来的人数**。老师肯回来动手练，就证明前面那段他看进去了；不回来，就是没看进去或者不需要我们。够用了。

**要敢看的失败信号**：如果第 8 周数据显示「外链点击量很高（比如周 800 次）但 7 日回站 SOP 转化 < 5%」，**这不是数据问题，是桥接假设不成立**——说明老师看完官方课就满足了，本站的 SOP 承接没有被需要。此时正确的动作是**收缩伴学页规模、把资源移回 SOP 生产**，而不是继续加课。这个判据要提前写死，否则到时候一定会有人解释成"再等等就好了"。

### 4.3 埋点事件清单（MVP）

| 事件名 | 触发时机 | 参数 | 验证的假设 | 优先级 |
|---|---|---|---|---|
| `literacy_page_view` | 三层页面任一加载完成 | `level`(landing\|module\|lesson)、`module_key`、`lesson_id`、`ref_kind`(direct\|search\|internal\|social) | 三层漏斗的分母；哪一层在漏人 | P0 |
| `lesson_view_30s` | 伴学页停留满 30 秒且页面可见 | `lesson_id`、`module_key` | 真读了导学（区分误点） | P0 |
| **`official_link_click`** | 点击外链按钮时（跳转前 beacon） | `lesson_id`、`module_key`、`stage`、`position`(hero\|bottom)、`duration_before_click` | **本板块第一指标：送达量**；`position` 用于判断按钮该放哪 | **P0** |
| `related_sop_click` | 伴学页点击关联 SOP | `lesson_id`、`sop_id`、`from`='literacy' | 「懂原理 → 学操作」承接是否成立（Q2） | P0 |
| **`sop_prompt_copied`** | SOP 页复制提示词（`copyText()` 内） | `sop_id`、`step_index`、`lit_ref_lesson_id`、`lit_ref_age_h` | **北极星 WAU-SOP 的唯一构成动作**（当前零埋点，必须新增） | **P0** |
| `module_empty_view` | 模块页空态曝光 | `module_key` | 哪个空模块被找得最多 → 直接决定下周先录哪个模块 | P1 |
| `official_link_error` | 巡检发现链接失效 | `lesson_id`、`status_code`\|`timeout` | 外链健康度（服务端事件，非用户事件） | P1 |
| `literacy_to_usages_exit` | 从空态出口跳去 `/usages` | `module_key`、`exit_type` | 空态是否成功兜住了流量 | P1 |

**明确不埋的**（说清楚不埋什么同样重要，避免开发过度实现）：
- 合规声明的曝光/点击 —— 无决策价值，埋了也不会改任何东西
- 页面滚动深度 —— 伴学页本身就短，无信息量
- 鼠标热力图 —— 种子期流量不足以形成有效热区，纯浪费

### 4.4 漏斗与第 12 周目标

```
模块页浏览
   ↓  ≥ 45%
伴学页浏览（literacy_page_view level=lesson）
   ↓  ≥ 60%
读满 30 秒（lesson_view_30s）
   ├── 外链点击 official_link_click        目标 ≥ 35%   ← 送达
   └── SOP 点击 related_sop_click          目标 ≥ 20%   ← 直接回收
   ↓  7 天归因窗口
sop_prompt_copied（带 lit_ref）           外链点击者 7 日回站转化 ≥ 12%
   ↓
literacy_assisted_WAU-SOP                 目标 ≥ 75 人 / 周（占 WAU-SOP 500 的 15%）
```

> 「占北极星 15%」这个数是这么定的：通识课是**五个内容板块之一**，但它是唯一有官方权威背书、也是唯一为 AISO 专门设计的资产，理应跑赢平均（20% 为均分）。定 15% 是留了容错。**低于 8% 就要认真考虑 Q1 的假设是否成立。**

### 4.5 埋点实现约束（贴着 1–2 人、近零预算写）

1. **不接第三方分析（GA / Mixpanel / 百度统计）。** 理由三条：境外服务在教育场景有数据出境合规问题；免费版采样会让个位数量级的数据完全失真；引入 SDK 会拖慢首屏，而 SEO 是本板块的核心资产。
2. **自建最小事件通道**，复用已有的 admin + Prisma 栈，新增一张表和一个接口即可：
   - `POST /api/events`（公开、无需登录、限流），落 `Event(id, name, did, props JSON, ts)`
   - `did` = 匿名设备 ID，客户端 UUID 存 localStorage（`ea_did`），**与账号体系解耦**
   - 客户端批量：`navigator.sendBeacon`，5 秒或攒满 10 条 flush；外链点击走单发立即上报（AC-15）
3. **隐私最小化（呼应 3.4）**：不采集 IP 原文、不采集 UA 原文（只存 `device: mobile|desktop`）、不采集任何用户输入内容与提示词正文（只存 `sop_id` + `step_index`）。
4. **必须覆盖匿名用户。** 这是与现有 `/api/me/*` 的关键区别——那套需要登录，只能看到极小一部分人；事件通道必须对未登录老师同样生效，否则北极星依旧算不出来。
5. **看数成本要低到会真的看。** 后台 `/admin/analytics` 只做一个页面：上面这个漏斗 + 单课转化排行榜（Q3 直接用），**不做自定义查询、不做图表配置器**。一个每周花 5 分钟能看完的页面，比一个功能齐全但没人打开的看板有用得多。

---

## 附：给架构师与设计师的三条硬输入

1. **数据模型**：`relatedSopIds` 存 `Usage.id`（= `SopPath.usageId`），不是 tool slug。这是 AC-17 修复的根，改 href 治标不治本。
2. **发布校验**：`published` 必须满足「`guideText` ≥ 40 字」+「`relatedSopIds` ≥ 1」。这两条硬校验是内容质量与合规抗风险（3.5-L2）的共同底座，不可放宽为警告。
3. **事件通道**：`POST /api/events` + `Event` 表 + `copyText()` 内的 `sop_prompt_copied` 上报，与本板块功能同批交付。否则第 12 周无法验证任何假设。



