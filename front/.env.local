# 前端取数缝配置（Plan B：全站后台驱动）
# 置为 remote 即走 admin 的 /api/content/*；置为其他值（或删除）则回退静态 data.ts
NEXT_PUBLIC_CONTENT_SOURCE=remote

# 后台 admin 源站（独立进程运行在 :3001）。同源部署时可留空。
NEXT_PUBLIC_API_BASE=http://10.9.0.10:3001
