# 前端取数配置
NEXT_PUBLIC_CONTENT_SOURCE=remote

# admin API 地址（生产环境用子域名，走 HTTPS，浏览器端取数使用）
NEXT_PUBLIC_API_BASE=http://124.221.235.198:3001

# 服务端 SSR 取数地址（内网直连 admin，不依赖公网 DNS / 回环）
# 仅在 Node 服务端生效；浏览器端无此变量，自动回落到上面的公网域名
INTERNAL_API_BASE=http://127.0.0.1:3001
