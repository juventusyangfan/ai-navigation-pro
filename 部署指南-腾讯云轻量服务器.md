# 教AI导航 · 双项目腾讯云轻量服务器部署指南

> 适用于：腾讯云轻量应用服务器 (Lighthouse) · Ubuntu 22.04/24.04 · Node.js 20 LTS

---

## 一、架构概述

```
                    Internet / 用户浏览器
                          │ HTTPS :443
                          ▼
          ┌─────────────────────────────────────────┐
          │     腾讯云轻量应用服务器 (Lighthouse)      │
          │                                           │
          │   ┌─────────────────────────────────┐    │
          │   │      Nginx 反向代理 (:80/:443)    │    │
          │   │   SSL 终端 · 按子域名分发请求       │    │
          │   └──────┬──────────────────┬────────┘    │
          │     www.eanavi.com   admin.eanavi.com     │
          │          │                  │            │
          │   ┌──────▼──────┐  ┌───────▼────────┐   │
          │   │ front (:3000)│  │ admin (:3001)   │   │
          │   │ Next.js 16   │  │ Next.js 15     │   │
          │   │ 前端展示站    │──│ 后台管理+API    │   │
          │   └──────────────┘  └───────┬────────┘   │
          │                            │             │
          │                    ┌───────▼────────┐    │
          │                    │ SQLite (dev.db) │    │
          │                    │ Prisma ORM      │    │
          │                    └────────────────┘    │
          │                                           │
          │   PM2 进程管理器守护两个 Node.js 进程       │
          └─────────────────────────────────────────┘
```

### 核心要点

| 组件 | 作用 | 端口 |
|------|------|------|
| Nginx | 反向代理 + SSL 终端 + 域名分发 | 80, 443 |
| front | 前端展示站 (Next.js 16) | 3000 |
| admin | 后台管理 + 内容API (Next.js 15 + Prisma) | 3001 |
| SQLite | 轻量数据库 (零外部依赖) | - |
| PM2 | Node.js 进程守护与自动重启 | - |

### 数据流

1. 用户访问 `www.eanavi.com` → Nginx → front (:3000) → 页面渲染
2. front 客户端 fetch `admin.eanavi.com/api/content/*` → Nginx → admin (:3001) → SQLite
3. 管理员访问 `admin.eanavi.com/admin` → Nginx → admin (:3001) → 后台管理界面

---

## 二、前置条件

### 2.1 服务器要求

| 项目 | 最低配置 | 推荐配置 |
|------|---------|---------|
| CPU | 2 核 | 2 核 |
| 内存 | 2 GB | 4 GB |
| 磁盘 | 40 GB SSD | 60 GB SSD |
| 带宽 | 4 Mbps | 6 Mbps |
| 系统 | Ubuntu 22.04 LTS | Ubuntu 24.04 LTS |

> 两个 Next.js 应用 + Nginx + PM2 同时运行，2GB 内存是底线。如果 build 过程中 OOM，请临时添加 swap。

### 2.2 域名要求

需要两个子域名解析到服务器公网 IP：

| 子域名 | 用途 |
|--------|------|
| `www.eanavi.com` (或 `eanavi.com`) | 前端展示站 |
| `admin.eanavi.com` | 后台管理 + API |

**DNS 配置**：在域名服务商处添加 A 记录：
```
类型  主机记录  记录类型  记录值
-    www      A        <你的服务器公网IP>
-    admin    A        <你的服务器公网IP>
-    @        A        <你的服务器公网IP>   (可选，用于裸域名跳转)
```

### 2.3 本地准备

确认项目已推送到 GitHub：https://github.com/juventusyangfan/ai-navigation-pro.git

---

## 三、服务器环境准备

SSH 登录服务器后，依次执行以下步骤。

### 3.1 系统更新

```bash
sudo apt update && sudo apt upgrade -y
```

### 3.2 安装 Node.js 20 LTS

```bash
# 添加 NodeSource 仓库
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -

# 安装 Node.js
sudo apt install -y nodejs

# 验证
node -v   # 应输出 v20.x.x
npm -v    # 应输出 10.x.x
```

### 3.3 安装 PM2（进程管理器）

```bash
sudo npm install -g pm2

# 设置 PM2 开机自启
pm2 startup systemd
# 按提示执行返回的 sudo 命令，类似：
# sudo env PATH=$PATH:/usr/bin pm2 startup systemd -u ubuntu --hp /home/ubuntu
```

### 3.4 安装 Nginx

```bash
sudo apt install -y nginx

# 启动并设置开机自启
sudo systemctl start nginx
sudo systemctl enable nginx
```

### 3.5 安装 Certbot（SSL 证书工具）

```bash
sudo apt install -y certbot python3-certbot-nginx
```

### 3.6 安装 Git

```bash
sudo apt install -y git
```

### 3.7 安装 build 工具（部分 npm 包需要编译）

```bash
sudo apt install -y build-essential
```

---

## 四、项目部署

### 4.1 创建项目目录

```bash
sudo mkdir -p /srv/app
sudo chown $USER:$USER /srv/app
```

### 4.2 克隆代码

```bash
cd /srv/app
git clone https://github.com/juventusyangfan/ai-navigation-pro.git
cd ai-navigation-pro
```

### 4.3 配置环境变量（关键步骤）

> **重要**：Next.js 的 `NEXT_PUBLIC_*` 变量在 **build 时** 内联到客户端代码中，必须在 `npm run build` 之前正确配置。

#### 4.3.1 front 项目环境变量

```bash
cat > /srv/app/ai-navigation-pro/front/.env.production << 'EOF'
# 前端取数配置
NEXT_PUBLIC_CONTENT_SOURCE=remote

# admin API 地址（生产环境用子域名，走 HTTPS，浏览器端取数使用）
NEXT_PUBLIC_API_BASE=https://admin.eanavi.com

# 服务端 SSR 取数地址（内网直连 admin，不依赖公网 DNS / 回环）
# 仅在 Node 服务端生效；浏览器端无此变量，自动回落到上面的公网域名
INTERNAL_API_BASE=http://127.0.0.1:3001
EOF
```

#### 4.3.2 admin 项目环境变量

```bash
# 生成强随机 AUTH_SECRET
AUTH_SECRET=$(openssl rand -base64 32)
echo "生成的 AUTH_SECRET: $AUTH_SECRET"
echo "请记录此值，后续配置需要用到"

cat > /srv/app/ai-navigation-pro/admin/.env << EOF
# 会话签名密钥（生产环境必须替换为强随机串）
AUTH_SECRET=$AUTH_SECRET

# 数据库（SQLite，零外部依赖）
DATABASE_URL="file:./dev.db"

# 公开站源站（用于 CORS 白名单，必须是 front 的完整 URL）
NEXT_PUBLIC_SITE_ORIGIN=https://www.eanavi.com

# Admin 监听端口
PORT=3001

# 采集中心 LLM 配置（按需修改）
COLLECT_LLM_BASE_URL=https://api.deepseek.com
COLLECT_LLM_API_KEY=你的API密钥
COLLECT_LLM_MODEL=deepseek-chat
EOF
```

> **注意**：`NEXT_PUBLIC_SITE_ORIGIN` 必须与 front 的实际访问地址完全一致（含协议、不含端口），否则 CORS 会拦截请求。

### 4.4 安装依赖

```bash
# admin 项目（先装，因为需要 prisma generate）
cd /srv/app/ai-navigation-pro/admin
npm install

# front 项目
cd /srv/app/ai-navigation-pro/front
npm install
```

> 如果服务器内存 < 4GB，npm install 可能较慢或失败。可临时添加 swap：
> ```bash
> sudo fallocate -l 2G /swapfile
> sudo chmod 600 /swapfile
> sudo mkswap /swapfile
> sudo swapon /swapfile
> ```

### 4.5 初始化数据库

```bash
cd /srv/app/ai-navigation-pro/admin

# 创建数据库表结构 + 写入种子数据
npm run db:init

# 验证数据库已创建
ls -la prisma/dev.db
```

> 如果你想迁移本地已有的数据，可以用 scp 上传本地的 `admin/prisma/dev.db`：
> ```bash
> # 在本地机器执行
> scp D:/project/ai-navigation-pro/admin/prisma/dev.db ubuntu@<服务器IP>:/srv/app/ai-navigation-pro/admin/prisma/dev.db
> ```

### 4.6 构建项目

```bash
# 构建 front
cd /srv/app/ai-navigation-pro/front
npm run build

# 构建 admin
cd /srv/app/ai-navigation-pro/admin
npm run build
```

> **构建顺序**：先 admin 后 front 或先 front 后 admin 均可，两者独立。
> **内存不足**：如果 build 时 OOM，添加 swap 后重试。

---

## 五、PM2 进程管理

### 5.1 更新 ecosystem 配置

项目已自带 `ecosystem.config.js`，但需要根据实际路径调整。编辑文件：

```bash
cat > /srv/app/ai-navigation-pro/ecosystem.config.js << 'EOF'
module.exports = {
  apps: [
    {
      name: "admin",
      cwd: "/srv/app/ai-navigation-pro/admin",
      script: "npm",
      args: "run start",
      env: {
        PORT: 3001,
        NODE_ENV: "production",
        DATABASE_URL: "file:/srv/app/ai-navigation-pro/admin/prisma/dev.db"
      },
      max_memory_restart: "500M",
      error_file: "/srv/app/ai-navigation-pro/logs/admin-error.log",
      out_file: "/srv/app/ai-navigation-pro/logs/admin-out.log",
      log_date_format: "YYYY-MM-DD HH:mm:ss"
    },
    {
      name: "front",
      cwd: "/srv/app/ai-navigation-pro/front",
      script: "npm",
      args: "run start",
      env: {
        PORT: 3000,
        NODE_ENV: "production"
      },
      max_memory_restart: "500M",
      error_file: "/srv/app/ai-navigation-pro/logs/front-error.log",
      out_file: "/srv/app/ai-navigation-pro/logs/front-out.log",
      log_date_format: "YYYY-MM-DD HH:mm:ss"
    }
  ]
};
EOF
```

### 5.2 创建日志目录

```bash
mkdir -p /srv/app/ai-navigation-pro/logs
```

### 5.3 启动应用

```bash
cd /srv/app/ai-navigation-pro
pm2 start ecosystem.config.js

# 查看状态
pm2 status

# 保存进程列表（开机自启）
pm2 save
```

### 5.4 常用 PM2 命令

```bash
pm2 status              # 查看所有进程状态
pm2 logs                # 查看实时日志
pm2 logs admin          # 只看 admin 日志
pm2 logs front          # 只看 front 日志
pm2 restart all         # 重启所有应用
pm2 restart admin       # 重启单个应用
pm2 stop all            # 停止所有应用
pm2 monit               # 实时监控面板（CPU/内存）
```

---

## 六、Nginx 反向代理配置

### 6.1 创建 front 站点配置

```bash
sudo cat > /etc/nginx/sites-available/eanavi-front << 'EOF'
# 前端展示站 - www.eanavi.com
server {
    listen 80;
    listen [::]:80;
    server_name www.eanavi.com eanavi.com;

    # HTTP 跳转 HTTPS（SSL 配置后启用）
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name www.eanavi.com eanavi.com;

    # SSL 证书路径（certbot 自动填充）
    # ssl_certificate     /etc/letsencrypt/live/www.eanavi.com/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/www.eanavi.com/privkey.pem;

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # Next.js 反向代理
    location / {
        proxy_pass http://127.0.0.1:3000;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Next.js 静态资源缓存
    location /_next/static/ {
        proxy_pass http://127.0.0.1:3000;
        proxy_cache_valid 200 365d;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}
EOF
```

### 6.2 创建 admin 站点配置

```bash
sudo cat > /etc/nginx/sites-available/eanavi-admin << 'EOF'
# 后台管理 + API - admin.eanavi.com
server {
    listen 80;
    listen [::]:80;
    server_name admin.eanavi.com;

    # HTTP 跳转 HTTPS（SSL 配置后启用）
    return 301 https://$host$request_uri;
}

server {
    listen 443 ssl;
    listen [::]:443 ssl;
    server_name admin.eanavi.com;

    # SSL 证书路径（certbot 自动填充）
    # ssl_certificate     /etc/letsencrypt/live/admin.eanavi.com/fullchain.pem;
    # ssl_certificate_key /etc/letsencrypt/live/admin.eanavi.com/privkey.pem;

    # 安全头
    add_header X-Frame-Options "SAMEORIGIN" always;
    add_header X-Content-Type-Options "nosniff" always;
    add_header X-XSS-Protection "1; mode=block" always;

    # 上传文件大小限制（admin 后台可能有图片上传）
    client_max_body_size 20M;

    # Next.js 反向代理
    location / {
        proxy_pass http://127.0.0.1:3001;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection 'upgrade';
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_cache_bypass $http_upgrade;
    }

    # Next.js 静态资源缓存
    location /_next/static/ {
        proxy_pass http://127.0.0.1:3001;
        proxy_cache_valid 200 365d;
        add_header Cache-Control "public, max-age=31536000, immutable";
    }
}
EOF
```

### 6.3 启用站点

```bash
# 创建软链接启用站点
sudo ln -sf /etc/nginx/sites-available/eanavi-front /etc/nginx/sites-enabled/
sudo ln -sf /etc/nginx/sites-available/eanavi-admin /etc/nginx/sites-enabled/

# 删除默认站点（避免冲突）
sudo rm -f /etc/nginx/sites-enabled/default

# 测试配置语法
sudo nginx -t

# 重载 Nginx
sudo systemctl reload nginx
```

---

## 七、SSL/HTTPS 配置

### 7.1 先确保 DNS 已生效

```bash
# 在本地或服务器上测试 DNS 解析
dig www.eanavi.com +short
dig admin.eanavi.com +short
# 两个都应返回你的服务器公网 IP
```

> DNS 生效通常需要几分钟到几小时。如果未生效，请等待后再继续。

### 7.2 申请 Let's Encrypt 证书

```bash
# 先注释掉 Nginx 配置中的 HTTPS server 块（return 301 会阻止 certbot 验证）
# 临时让 HTTP 直接代理到应用

# 方法：先修改 front 和 admin 配置，把 return 301 那行注释掉
# 然后重载 nginx
sudo nginx -t && sudo systemctl reload nginx

# 申请证书（certbot 会自动修改 Nginx 配置）
sudo certbot --nginx -d www.eanavi.com -d eanavi.com -d admin.eanavi.com \
  --non-interactive --agree-tos -m your-email@example.com --redirect

# 证书申请成功后，certbot 会自动：
# 1. 填充 ssl_certificate 和 ssl_certificate_key 路径
# 2. 启用 HTTP → HTTPS 跳转
```

> **注意**：请将 `your-email@example.com` 替换为你的真实邮箱。

### 7.3 设置自动续期

```bash
# 测试续期流程
sudo certbot renew --dry-run

# certbot 会自动添加定时任务（systemd timer），无需手动配置
# 证书有效期 90 天，到期前 30 天自动续期
```

### 7.4 验证 HTTPS

```bash
curl -I https://www.eanavi.com    # 应返回 200 或 302
curl -I https://admin.eanavi.com  # 应返回 200 或 302
```

---

## 八、防火墙与安全配置

### 8.1 腾讯云安全组

在腾讯云控制台 → 轻量应用服务器 → 防火墙，开放以下端口：

| 端口 | 协议 | 来源 | 用途 |
|------|------|------|------|
| 22 | TCP | 你的 IP | SSH |
| 80 | TCP | 0.0.0.0/0 | HTTP (跳转 HTTPS) |
| 443 | TCP | 0.0.0.0/0 | HTTPS |

> **不要**对外开放 3000 和 3001 端口，它们只通过 Nginx 内部代理访问。

### 8.2 UFW 防火墙（系统层）

```bash
sudo ufw allow 22/tcp
sudo ufw allow 80/tcp
sudo ufw allow 443/tcp
sudo ufw --force enable
sudo ufw status
```

### 8.3 SSH 安全加固（推荐）

```bash
# 禁用密码登录，仅允许密钥登录（确保已配置 SSH 密钥后再执行）
sudo sed -i 's/#PasswordAuthentication yes/PasswordAuthentication no/' /etc/ssh/sshd_config
sudo systemctl restart sshd
```

### 8.4 安装 Fail2ban（防暴力破解）

```bash
sudo apt install -y fail2ban
sudo systemctl enable fail2ban
sudo systemctl start fail2ban
```

---

## 九、验证与测试

### 9.1 检查所有服务状态

```bash
# PM2 进程
pm2 status
# 期望：admin 和 front 都是 online

# Nginx
sudo systemctl status nginx
# 期望：active (running)

# 测试本地连通
curl -s http://127.0.0.1:3000 | head -5    # front 应返回 HTML
curl -s http://127.0.0.1:3001/api/content/tools | head -5  # admin API 应返回 JSON
```

### 9.2 测试公网访问

```bash
# 前端站
curl -I https://www.eanavi.com
# 期望：HTTP/2 200

# admin API
curl -s https://admin.eanavi.com/api/content/tools | python3 -m json.tool | head -20
# 期望：返回工具列表 JSON

# CORS 验证（模拟前端跨域请求）
curl -s -I -X OPTIONS \
  -H "Origin: https://www.eanavi.com" \
  -H "Access-Control-Request-Method: GET" \
  https://admin.eanavi.com/api/content/tools
# 期望：响应头包含 Access-Control-Allow-Origin: https://www.eanavi.com
```

### 9.3 浏览器验证清单

打开浏览器，逐项检查：

- [ ] `https://www.eanavi.com` — 首页正常加载，场景卡片、工具列表有数据
- [ ] `https://www.eanavi.com/scenes` — 场景列表页正常
- [ ] `https://www.eanavi.com/tool/` — 工具详情页正常（SOP 步骤有内容）
- [ ] `https://www.eanavi.com/usages` — 用法库正常
- [ ] `https://www.eanavi.com/login` — 用户登录功能正常
- [ ] `https://admin.eanavi.com/admin/login` — 后台登录页正常
- [ ] 后台登录后能进入管理界面
- [ ] 修改 admin 后台数据 → 刷新前端页面数据同步更新

---

## 十、日常维护与备份

### 10.1 SQLite 数据库备份

```bash
# 创建备份脚本
sudo cat > /srv/app/ai-navigation-pro/backup-db.sh << 'SCRIPT'
#!/bin/bash
BACKUP_DIR="/srv/app/ai-navigation-pro/backups"
DB_FILE="/srv/app/ai-navigation-pro/admin/prisma/dev.db"
TIMESTAMP=$(date +"%Y%m%d_%H%M%S")

mkdir -p "$BACKUP_DIR"
sqlite3 "$DB_FILE" ".backup '$BACKUP_DIR/dev_$TIMESTAMP.db'"

# 只保留最近 30 天的备份
find "$BACKUP_DIR" -name "dev_*.db" -mtime +30 -delete

echo "[$(date)] 备份完成: dev_$TIMESTAMP.db"
SCRIPT

sudo chmod +x /srv/app/ai-navigation-pro/backup-db.sh

# 安装 sqlite3（如果没有）
sudo apt install -y sqlite3

# 手动执行一次备份测试
/srv/app/ai-navigation-pro/backup-db.sh
```

### 10.2 设置定时备份

```bash
# 每天凌晨 3 点自动备份
crontab -e
# 添加以下行：
0 3 * * * /srv/app/ai-navigation-pro/backup-db.sh >> /srv/app/ai-navigation-pro/logs/backup.log 2>&1
```

### 10.3 更新部署（代码更新后）

```bash
cd /srv/app/ai-navigation-pro

# 拉取最新代码
git pull origin main

# 更新依赖（如果 package.json 有变化）
cd front && npm install && cd ..
cd admin && npm install && cd ..

# 重新构建（NEXT_PUBLIC_ 变量变更时必须重新 build）
cd front && npm run build && cd ..
cd admin && npm run build && cd ..

# 如果数据库 schema 有变化
cd admin && npm run db:push && cd ..

# 重启 PM2
pm2 restart all

# 验证
pm2 status
```

### 10.4 日志管理

```bash
# PM2 日志（自动轮转）
pm2 install pm2-logrotate
pm2 set pm2-logrotate:max_size 10M
pm2 set pm2-logrotate:retain 7

# Nginx 日志
sudo tail -f /var/log/nginx/access.log
sudo tail -f /var/log/nginx/error.log
```

---

## 十一、常见问题排查

### Q1: 前端页面加载但数据为空

**原因**：front 无法访问 admin API，通常是 CORS 或网络问题。

**排查**：
```bash
# 1. 检查 admin 是否在运行
pm2 status

# 2. 测试 admin API 本地连通
curl http://127.0.0.1:3001/api/content/tools

# 3. 检查 admin 的 NEXT_PUBLIC_SITE_ORIGIN 是否正确
cat /srv/app/ai-navigation-pro/admin/.env | grep SITE_ORIGIN
# 必须是 https://www.eanavi.com（与前端实际访问地址完全一致）

# 4. 浏览器控制台查看 CORS 错误
# 如果报 CORS 错误，说明 NEXT_PUBLIC_SITE_ORIGIN 不匹配
```

**修复**：
```bash
# 修改 admin/.env 中的 NEXT_PUBLIC_SITE_ORIGIN
# 然后重新 build admin（因为 NEXT_PUBLIC_ 是 build 时注入的）
cd /srv/app/ai-navigation-pro/admin
npm run build
pm2 restart admin
```

### Q2: build 时内存不足 (OOM)

```bash
# 添加临时 swap
sudo fallocate -l 4G /swapfile
sudo chmod 600 /swapfile
sudo mkswap /swapfile
sudo swapon /swapfile

# 重新 build
npm run build

# build 完成后可移除 swap（可选）
# sudo swapoff /swapfile && sudo rm /swapfile
```

### Q3: PM2 进程频繁重启

```bash
# 查看错误日志
pm2 logs admin --err --lines 50
pm2 logs front --err --lines 50

# 常见原因：
# 1. 端口被占用 → 检查 lsof -i :3000 / lsof -i :3001
# 2. 环境变量缺失 → 检查 .env 文件
# 3. 内存不足 → 检查 free -h，调大 max_memory_restart
```

### Q4: SSL 证书过期

```bash
# 检查证书状态
sudo certbot certificates

# 手动续期
sudo certbot renew
sudo systemctl reload nginx
```

### Q5: 前端 Next.js 16 构建报错

Next.js 16 需要 Node.js 18.18+，确认版本：
```bash
node -v
# 如果低于 18.18，升级 Node.js
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs
```

### Q6: 数据库被锁 (database is locked)

SQLite 在高并发写入时可能出现锁问题：
```bash
# 重启 admin 应用释放锁
pm2 restart admin

# 如果频繁出现，考虑在 prisma schema 中添加 WAL 模式
# 在 admin/prisma/schema.prisma 的 datasource 块后添加：
# // SQLite WAL 模式提升并发读性能
```

---

## 十二、部署速查表

以下是完整部署的一键流程（从零开始）：

```bash
# === 1. 系统准备 ===
sudo apt update && sudo apt upgrade -y
curl -fsSL https://deb.nodesource.com/setup_20.x | sudo -E bash -
sudo apt install -y nodejs nginx git build-essential certbot python3-certbot-nginx sqlite3
sudo npm install -g pm2
pm2 startup systemd

# === 2. 克隆项目 ===
sudo mkdir -p /srv/app && sudo chown $USER:$USER /srv/app
cd /srv/app && git clone https://github.com/juventusyangfan/ai-navigation-pro.git

# === 3. 配置环境变量 ===
# (参考第四节，务必在 build 前配置好 .env 文件)

# === 4. 安装依赖 + 初始化数据库 ===
cd /srv/app/ai-navigation-pro/admin && npm install && npm run db:init
cd /srv/app/ai-navigation-pro/front && npm install

# === 5. 构建 ===
cd /srv/app/ai-navigation-pro/admin && npm run build
cd /srv/app/ai-navigation-pro/front && npm run build

# === 6. 启动 PM2 ===
mkdir -p /srv/app/ai-navigation-pro/logs
cd /srv/app/ai-navigation-pro
# (编辑 ecosystem.config.js 后)
pm2 start ecosystem.config.js && pm2 save

# === 7. 配置 Nginx ===
# (参考第六节创建配置文件)
sudo nginx -t && sudo systemctl reload nginx

# === 8. 申请 SSL ===
sudo certbot --nginx -d www.eanavi.com -d eanavi.com -d admin.eanavi.com \
  --non-interactive --agree-tos -m your-email@example.com --redirect

# === 9. 防火墙 ===
sudo ufw allow 22/tcp && sudo ufw allow 80/tcp && sudo ufw allow 443/tcp
sudo ufw --force enable

# === 10. 验证 ===
pm2 status
curl -I https://www.eanavi.com
curl -s https://admin.eanavi.com/api/content/tools | head -5
```

---

## 附录：项目结构速览

```
ai-navigation-pro/
├── ecosystem.config.js          # PM2 配置
├── front/                        # 前端展示站 (Next.js 16)
│   ├── .env.production           # 生产环境变量（部署时创建）
│   ├── next.config.ts
│   └── src/
│       ├── lib/content.ts        # 取数缝（调 admin API）
│       ├── lib/auth.ts           # 前台用户会话
│       └── app/                  # 页面路由
│
├── admin/                        # 后台管理+API (Next.js 15)
│   ├── .env                      # 环境变量（部署时配置）
│   ├── next.config.mjs
│   ├── prisma/
│   │   ├── schema.prisma         # 数据库模型
│   │   ├── seed.ts               # 种子数据
│   │   └── dev.db                # SQLite 数据库文件
│   └── src/
│       ├── middleware.ts          # 后台路由保护
│       ├── lib/
│       │   ├── http.ts            # CORS + API 工具函数
│       │   ├── db.ts              # Prisma 客户端
│       │   └── auth.ts            # 会话管理
│       └── app/api/
│           ├── content/           # 公开内容 API
│           ├── auth/              # 用户认证 API
│           ├── me/                # 用户个人数据 API
│           └── admin/             # 后台管理 API（需登录）
│
└── logs/                         # PM2 日志目录（部署时创建）
```

---

> **最后提醒**：`NEXT_PUBLIC_*` 环境变量在 build 时就固化到代码里了，改完必须重新 `npm run build` + `pm2 restart`。这是部署中最容易踩的坑。
